"use client";

/** Siparişlerim row: checkout prefs shell (all lines extracted to dedicated components). */
export function MyOrderPrefLines({ note }: { note?: string }) {
  void note;
  return null;
}
